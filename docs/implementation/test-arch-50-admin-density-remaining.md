# TEST-ARCH-50 - Admin density remaining batch

Moved four remaining admin enterprise-density UI tests from root test/ into test/unit/ui/admin/.

Pre-scout found no real relative module imports; one match was a string assertion only.

Guardrails: no runtime/product/API/auth/DB/schema/migration/dependency/lockfile/CI changes.
