# QGA Workflow Security Positive Canary

Temporary canary used to verify that the trusted `pull_request_target` workflow publishes the `qga-workflow-security` check on the pull request head SHA through the dedicated governance GitHub App.

This branch is not intended to merge. Close the canary PR after the check is observed and verified.
