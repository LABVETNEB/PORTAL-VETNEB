{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": ["node", "express"],
    "noEmit": true,
    "rootDir": ".."
  },
  "include": [
    "./**/*.ts",
    "../server/**/*.ts",
    "../drizzle/**/*.ts",
    "../*.ts"
  ]
}
